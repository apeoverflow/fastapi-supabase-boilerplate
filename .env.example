# =============================================================================
# FastAPI + Supabase Boilerplate - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# 🔑 SUPABASE CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Get these from your Supabase project dashboard:
# 1. Go to https://app.supabase.com/project/your-project/settings/api
# 2. Copy the Project URL and anon/public key

# Your Supabase project URL
SUPABASE_PROJECT_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (safe to use in client-side code)
SUPABASE_API_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key

# Optional: Supabase service role key (only if needed for admin operations)
# SUPABASE_PW=your-service-role-key

# -----------------------------------------------------------------------------
# 🚀 SERVER CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# These settings control how the FastAPI server runs

# Host to bind the server to (default: 0.0.0.0)
HOST=0.0.0.0

# Port to run the server on (default: 8000)
PORT=8000

# Enable auto-reload during development (default: true)
# Set to false in production
RELOAD=true

# Logging level (default: info)
# Options: debug, info, warning, error, critical
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# 🔧 APPLICATION CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Additional settings for your application

# Environment (development, staging, production)
ENVIRONMENT=development

# API Version (for versioning your API)
API_VERSION=v1

# Maximum number of items per page (for pagination)
MAX_PAGE_SIZE=1000

# Default page size (for pagination)
DEFAULT_PAGE_SIZE=100

# -----------------------------------------------------------------------------
# 🔐 SECURITY SETTINGS (Optional)
# -----------------------------------------------------------------------------
# Security-related configuration

# Secret key for JWT tokens (generate a random string)
# SECRET_KEY=your-secret-key-here

# Token expiration time in minutes
# TOKEN_EXPIRE_MINUTES=60

# CORS origins (comma-separated list)
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# -----------------------------------------------------------------------------
# 📊 MONITORING & OBSERVABILITY (Optional)
# -----------------------------------------------------------------------------
# Settings for monitoring and logging

# Enable request logging
ENABLE_REQUEST_LOGGING=true

# Log format (json, text)
LOG_FORMAT=json

# Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project

# -----------------------------------------------------------------------------
# 🧪 TESTING & DEVELOPMENT (Optional)
# -----------------------------------------------------------------------------
# Settings for testing and development

# Test database URL (if different from main database)
# TEST_DATABASE_URL=postgresql://user:pass@localhost/test_db

# Enable debug mode
DEBUG=false

# Enable SQL query logging
# LOG_SQL_QUERIES=false

# -----------------------------------------------------------------------------
# 📝 INSTRUCTIONS
# -----------------------------------------------------------------------------
# 1. Copy this file to .env in your project root
# 2. Update the SUPABASE_* variables with your actual Supabase credentials
# 3. Modify other settings as needed for your environment
# 4. Never commit your .env file to version control
# 5. Keep this .env.example file updated when adding new variables

# ✅ Example of a minimal working configuration:
# SUPABASE_PROJECT_URL=https://abcdefghijklmnop.supabase.co
# SUPABASE_API_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYzMjUwNzI2MCwiZXhwIjoxOTQ4MDgzMjYwfQ.example-key
